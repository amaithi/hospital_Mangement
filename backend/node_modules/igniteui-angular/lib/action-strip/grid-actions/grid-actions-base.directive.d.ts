import { IgxActionStripComponent } from '../action-strip.component';
import { IgxRowDirective } from '../../grids/public_api';
export declare class IgxGridActionsBaseDirective {
    protected strip: IgxActionStripComponent;
    constructor(strip: IgxActionStripComponent);
    /**
     * Getter to be used in template
     * @hidden
     * @internal
     */
    get isRowContext(): boolean;
    /**
     * Check if the param is a row from a grid
     * @hidden
     * @internal
     * @param context
     */
    protected isRow(context: any): context is IgxRowDirective<any>;
}
